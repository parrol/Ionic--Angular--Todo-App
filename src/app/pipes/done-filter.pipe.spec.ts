import { DoneFilterPipe } from './done-filter.pipe';

describe('DoneFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoneFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
